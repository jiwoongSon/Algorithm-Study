#  0426 콜라츠 추측

## 풀이
입력 된 수(int num)가 
짝수인 경우 -> /2
홀수인 경우 -> *3 + 1

주어진 수가 언제 1이 되는지, count return하기
만약 500번이 넘는다면, -1 

이전 문제와 마찬가지로, while,if loop로는 해당 로직을 구현할 수 없다. -> 재귀함수 떠올리기


## 생각해볼 점
cnt 초기화 위치 잘 보기
재귀함수에서, 각 조건 이후 값은 return 키워드를 사용하여 작성 -> 다음 call스택으로 값을 *전달*해야한다. 

**인스턴스 변수로 count 등 변수를 사용하지 않는게 좋다. TestCase에서 연속적으로 사용된다. Solution 객체는 한번만 생성하므로** -> 변환해야 하면, 새로운 함수를 만들자 


## 개선할 점(코드)
## 성능